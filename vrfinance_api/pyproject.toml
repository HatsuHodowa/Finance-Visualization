[project]
name = "vrfinance-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=5.2.7",
]

[dependency-groups]
dev = [
    "django-debug-toolbar>=6.0.0",
]
